{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": ".eleventy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "eleventy --input=src/ --watch --serve --port=8080",
    "build": "eleventy --input=src/ --output=dist/ ",
    "deploy": "git push origin `git subtree split --prefix dist dev`:master --force"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy-plugin-rss": "^1.0.5",
    "debug": "^4.1.1",
    "markdown-it": "^8.4.2",
    "markdown-it-katex": "^2.0.3",
    "markdown-it-mathjax": "^2.0.0",
    "markdown-it-prism": "^2.0.1"
  },
  "devDependencies": {
    "markdown-it-classy": "^0.2.0"
  }
}
