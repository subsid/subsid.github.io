{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": ".eleventy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "eleventy --input=src/ --watch --serve --port=8080",
    "deploy": "eleventy --input=src/ --output=dist/ && git subtree push --prefix dist origin master"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy-plugin-rss": "^1.0.5",
    "debug": "^4.1.1",
    "markdown-it": "^8.4.2",
    "markdown-it-katex": "^2.0.3",
    "markdown-it-mathjax": "^2.0.0",
    "markdown-it-prism": "^2.0.1"
  }
}
